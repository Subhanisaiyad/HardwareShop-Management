HardwareShop Management System — a simple desktop application to automate shop tasks: maintain product inventory, create bills, track sales, and generate basic reports. Open the solution in Visual Studio, configure the database connection in configuration file, build and run the application.
